# -SQL-Basics-Filtering-Sorting-Aggregations
This task involves importing the Superstore CSV into a SQL table and performing basic SQL operations such as filtering with WHERE, sorting using ORDER BY, aggregations with SUM, AVG, and COUNT, grouping data, applying HAVING, and exporting summarized results to CSV for reporting.
